[Service]
# HUI requests a 10 Hz telemetry view. Physical UART sampling is serialized,
# therefore OqlOS serves the latest bounded sample while one refresh runs.
Environment=OQLOS_USB_ADC_SAMPLE_INTERVAL_SECONDS=0.1
Environment=OQLOS_USB_ADC_MAX_STALE_SECONDS=1.0
